<h2>Pet Pal booking</h2>

<form action="<%= pet_bookings_path(@pet) %>" method="post">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <label for="dates">Dates</label>
  <input type="text" name="booking[booking_date]" id="flatpickr" ref="flatpickr" placeholder="Select a date" />
  <input type="submit" name="" value="submit">
</form>
